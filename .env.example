CORS_ORIGINS=*
PORT=8000
# Optional: path to an Netscape-format cookies.txt file (for restricted Facebook/Instagram videos)
COOKIE_FILE=
# Optional: browser name to import cookies from, e.g. chrome, firefox, edge
COOKIES_FROM_BROWSER=
